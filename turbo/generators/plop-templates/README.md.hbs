# {{displayName}}

This is a {{#if (eq type "package")}}package{{else}}app{{/if}} workspace.

## Getting Started

To get started with this {{#if (eq type "package")}}package{{else}}app{{/if}}, run:

```bash
pnpm dev
```

## Building

To build this {{#if (eq type "package")}}package{{else}}app{{/if}}, run:

```bash
pnpm build
```
