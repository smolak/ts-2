{
  "name": "{{packageName}}",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Add dev script here'",
    "build": "echo 'Add build script here'",
    "test": "{{#if isTestPackage}}vitest run{{else}}echo 'Add test script here'{{/if}}",
    "lint": "echo 'Add lint script here'"
  }{{#if (eq type "package")}},
  "exports": {
    "./*": "./src/*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "catalog:"{{#if isTestPackage}},
    "vitest": "catalog:"{{/if}}
  }{{/if}}
}
